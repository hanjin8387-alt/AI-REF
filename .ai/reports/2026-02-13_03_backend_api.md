# Backend/API Report – 2026-02-13 (Feature Enhancement Cycle)

## Summary
- **검토 엔드포인트**: 21개 (6 라우터)
- **발견 항목**: 🔴 Critical: 3 / 🟡 Warning: 5 / 🟢 Info: 3

---

## Findings

### 🔴 Critical

#### BA-001: Admin 토큰 비교 timing-safe 미적용
- **파일**: `prometheus-api/app/api/admin.py` L28
- **근거**: `x_admin_token != token` 사용. `security.py`는 `secrets.compare_digest()` 사용.
- **권장 조치**: `secrets.compare_digest(x_admin_token, token)` + `import secrets`
- **위험도**: 🔴

#### BA-002: 스캔 업로드 시 파일 전체 메모리 로드 후 크기 검증
- **파일**: `prometheus-api/app/api/scans.py` L287-290
- **근거**: `await file.read()` 이후 `len(image_bytes) > max_bytes` 체크
- **권장 조치**: chunk 읽기 방식, 초과 시 조기 중단 (HTTP 413)
- **위험도**: 🔴

#### BA-003: `_normalize_storage_category` 함수 중복/불일치
- **파일**: `scans.py` L40-54, `inventory.py` L26-61
- **근거**: 동일 목적 함수가 다른 구현. `inventory.py`에는 `STORAGE_FALLBACK_MAP` 추가.
- **권장 조치**: `services/storage_utils.py`로 추출·통합
- **위험도**: 🔴

### 🟡 Warning

#### BA-004: `SELECT *` 남용
- **파일**: `inventory_service.py` L89, `scans.py` L371, `admin.py` L52 등
- **권장 조치**: 필요 컬럼만 명시

#### BA-005: `shopping.py` 734줄 과대
- **권장 조치**: `services/shopping_service.py`로 헬퍼 분리

#### BA-006: 에러 메시지 한/영 혼재
- **파일**: 백엔드 HTTPException 전반
- **근거**: `"이미지 파일만 업로드 가능합니다"` vs `"Invalid admin token"` 혼재
- **권장 조치**: 영어 통일, 프론트에서 `localizeServerError()` 활용

#### BA-007: Gemini 모델 폴백 로깅 수준 부적절
- **파일**: `gemini_service.py`
- **근거**: 모델 NotFound 첫 실패를 `logger.info()` — `logger.warning()` 필요

#### BA-008: `_normalize_unit` / `_normalize_category` 유틸도 파일별 중복
- **파일**: `scans.py`, `inventory.py`, `shopping.py`
- **권장 조치**: 공통 `services/normalization_utils.py` 추출

### 🟢 Info

#### BA-009: `os.path.basename` → `pathlib` 전환 권장
- **파일**: `scans.py` L2, L121

#### BA-010: API 응답에 일관된 envelope 패턴 미적용
- 일부는 `{"items": [...]}`, 일부는 raw list
- 향후 표준 envelope 도입 검토

#### BA-011: `STORAGE_CATEGORIES` 상수 tuple vs set 타입 불일치
- `scans.py` (tuple), `inventory.py` (set)

---

## API Contract Gaps
| Method | Path | 현재 문제 | 권장 수정 |
|--------|------|----------|----------|
| ALL | `/api/*` | 버전 없음 | `/api/v1/*` 도입 검토 |
| GET | `/health` | 미존재 | DB ping + 상태 반환 |
| ALL | 에러 응답 | 언어 불일치 | 영어 에러코드 통일 |

## Task List
| # | 파일 | 변경요지 | 테스트 커맨드 | 수용기준 | 위험도 |
|---|------|---------|-------------|---------|--------|
| 1 | `admin.py` | timing-safe 토큰 비교 | `pytest tests/test_admin.py -v` | `secrets.compare_digest` 사용 | 🔴 |
| 2 | `scans.py` | 스트리밍 크기 검증 | `pytest tests/test_scans.py -v` | chunk 방식, 413 반환 | 🔴 |
| 3 | `services/storage_utils.py` (신규) | 정규화 함수 통합 | `pytest tests/test_services/test_storage_utils.py -v` | 단일 진입점 | 🔴 |
| 4 | 백엔드 전반 | SELECT * 제거 | `pytest tests/ -v` | 명시 컬럼 | 🟡 |
| 5 | `services/shopping_service.py` (신규) | shopping 헬퍼 분리 | `pytest tests/ -v` | shopping.py ≤ 400줄 | 🟡 |
| 6 | 백엔드 전반 | 에러 메시지 영어 통일 | `pytest tests/ -v` | 한국어 에러 0건 | 🟡 |
| 7 | `gemini_service.py` | 폴백 로깅 warning 상향 | `pytest tests/ -v` | warning 로그 확인 | 🟡 |
| 8 | `services/normalization_utils.py` (신규) | unit/category 정규화 통합 | `pytest tests/ -v` | 중복 함수 0개 | 🟡 |

## Risk & Rollback
- BA-001: 1줄 수정, 즉시 롤백 가능
- BA-002: chunk 방식 전환은 API 동작 동일 (내부 리팩토링)
- BA-003/008: 추출 리팩토링, import 경로 변경으로 인한 깨짐 가능 → 테스트 게이트 필수
